---
import { PLACES } from "../../../data/places";

export function getStaticPaths() {
  return PLACES.map((p) => ({ params: { slug: p.slug } }));
}

const { slug } = Astro.params;
return Astro.redirect(`/london/therapy-and-wellbeing/${slug}`, 301);
---
